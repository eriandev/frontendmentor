---
import type { HTMLAttributes } from 'astro/types'

export interface LinkProps extends Omit<HTMLAttributes<'a'>, 'href'> {
  to: string | URL
  external?: boolean
}

const { to, title, external = false, ...restProps }: LinkProps = Astro.props
const rel = external ? 'noopener noreferrer' : null
const target = external ? '_blank' : null
---

<a {rel} {target} {title} href={to} {...restProps}>
  <slot />
</a>
